# Architecture

The architecture is documented as arc42 chapters in `./docs/arc42/` using the arc42-language CLI itself (dogfooding).

## Notes for LLM

- Use the arc42 CLI to read the docs: `pnpm arc42 --dir docs/arc42 get`
- Do not duplicate architecture content here — the `.arc42.md` files are the source of truth
- Use `pnpm arc42 --dir docs/arc42 validate` after any architectural changes
